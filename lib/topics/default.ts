/**
 * The default topic, Ogólne (spec 2026-09-19-topic-items §3.2). Migration 005
 * creates it; every card without a topic of its own is filed here. It cannot
 * be renamed or generated for, and has no context.
 */
export const DEFAULT_TOPIC_ID = 'default'
